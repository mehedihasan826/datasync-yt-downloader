server:
  port: ${SERVER_PORT:8765}

spring:
  application:
    name: datasync-yt-downloader
  jackson:
    serialization:
      write-dates-as-timestamps: false

logging:
  level:
    com.datasync.ytdownloader: INFO
    org.springframework: INFO
    org.telegram: WARN
